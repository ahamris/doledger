# Baseline: <hostname>

**Date:** YYYY-MM-DD  
**Hostname:** (e.g. workspace-01, cl-pve-00)  
**Role:** (e.g. Proxmox host, laptop, CI runner)

---

## Hardware / OS

- **CPU:** (model, cores)
- **Memory:** (RAM)
- **Disk:** (storage layout)
- **Kernel / OS:** (version)

## Network

- **Main interfaces:** (IPs, VLANs, bridges)
- **Key routes or constraints:** (optional)

## Constraints

Things that must not be changed without approval (e.g. ZFS pool names, static IPs, service ports).

## Key paths

| Path | Purpose |
|------|---------|
| `/opt` | (e.g. config, changelog, templates) |
| `/doledger` | This control plane (ledger, baseline, templates, log) |

## Services / IDs

(If applicable: CT/VM IDs, key ports, critical services.)

---

For full configuration details, see the host's main config (e.g. `/opt/baseline/configuration.md`) if present.
